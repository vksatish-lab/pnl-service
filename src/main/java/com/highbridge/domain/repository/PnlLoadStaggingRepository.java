package com.highbridge.domain.repository;

import com.highbridge.domain.PnlLoadStagging;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PnlLoadStaggingRepository extends JpaRepository<PnlLoadStagging, Long> {
}
